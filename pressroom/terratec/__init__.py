"""TerraTec, over five generations of press site.

None of them exists any more, so every one is recovered from the Wayback
Machine. They span a hand-authored 1996 page of anchors, two table-layout
sites with no class or id worth keying on, a PHP-Nuke portal in two languages,
and a 2007-2013 CMS whose listing carries the full release in its later years
and truncates it in its earlier ones.

The German and English tags are kept apart wherever the text genuinely differs,
which is not the same decision as the domain-only splits elsewhere.
"""
