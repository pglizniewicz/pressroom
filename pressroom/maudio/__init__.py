"""Midiman, then M-Audio, over five CMS generations and four domains.

The domains mirrored each other through the rename, so one CMS generation
stamps several tags and a file read off a sibling domain is not a cross-source
claim. Duplication across tags is intended: the same release lives on several
mirrors under unrelated url schemes and their bodies are separate extractions.

Within one tag the same release sometimes exists twice because the CMS
published it under two url schemes on one domain, one copy a listing blurb and
the other the full article. Those are paired and filled, never merged: both
urls really existed, so both rows stay.

This is also the only firm here whose releases arrive as .pdf and .doc
attachments.
"""
